package trial_debug;
/*

package tryt;

import Game.*;

public class pt {
	Tile tile1=new Tile();
	Board b=new Board();
	boolean validCood=true;
	
	public void game() 
	{
		if (b.getBoard1()[tile1.getX()][tile1.getY()] >= 3)
		{
	        if (b.getBoard1()[tile1.getX()][tile1.getY()] == 5)
	             System.out.println("You have guessed these coordinates already! Try Again");
	        
	        if (b.getBoard1()[tile1.getX()][tile1.getY()]==4)
	             System.out.println("Your sunken ship is there! Try Again");
	         
	        if (b.getBoard1()[tile1.getX()][tile1.getY()] == 8) 
	        {
	             System.out.println("You missed");
	             b.getBoard1()[tile1.getX()][tile1.getY()] = 5;
	             validCood = true;
	        }
	 	}
	}
}

*/